---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-block px-4 py-1.5 text-sm font-medium rounded-full border border-[var(--text-color)]/20 bg-[var(--text-color)]/5 opacity-90 whitespace-nowrap">
  {name}
</span>
