---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-block px-3 py-1 font-mono text-xs font-semibold uppercase tracking-wider rounded-full border border-[var(--text-color)]/20 bg-[var(--text-color)]/5 opacity-90 whitespace-nowrap">
  {name}
</span>
